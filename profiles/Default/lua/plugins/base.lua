-- /\/\/\/\ Default profile
--
-- This is the default profile, intended as a starting point for new profiles.
-- It ships with a working theme, LSP setup, and example plugins.
--
-- To create a new profile based on this one, run :NewProfile <name> inside Neovim.
--
-- Add the following alias to your ~/.bashrc:
--
-- Default() { echo "nvim-default" > "$NVIM_PROFILE_FILE"; NVIM_APPNAME="nvim-default" command nvim "$@"; }
-- Highly recommended trying to customise Misc profile for practice ;)

return {}
